name: CI

on:
  pull_request:
    paths:
      - 'docs/**'
      - '**.md'
  push:
    paths:
      - 'docs/**'
      - '**.md'

jobs:
  UTTEST:
    runs-on:
      - ubuntu-latest
    steps:
      - run: 'echo "No run required"'

  APITEST_DB:
    runs-on:
      - ubuntu-latest
    steps:
      - run: 'echo "No run required"'

  APITEST_DB_PROXY_CACHE:
    runs-on:
      - ubuntu-latest
    steps:
      - run: 'echo "No run required"'

  APITEST_LDAP:
    runs-on:
      - ubuntu-latest
    steps:
      - run: 'echo "No run required"'

  OFFLINE:
    runs-on:
      - ubuntu-latest
    steps:
      - run: 'echo "No run required"'

  UI_UT:
    runs-on:
      - ubuntu-latest
    steps:
      - run: 'echo "No run required"'
